<reactiveUi:ReactiveUserControl x:TypeArguments="viewModels:TimerViewModel"
                                x:Class="Toggl.WPF.Views.TimerView"
                                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                                xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
                                xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                                xmlns:local="clr-namespace:Toggl.WPF.Views"
                                xmlns:reactiveUi="http://reactiveui.net"
                                xmlns:viewModels="clr-namespace:Toggl.Core.UI.ViewModels;assembly=Toggl.Core.UI"
                                mc:Ignorable="d" 
                                d:DesignHeight="100" d:DesignWidth="400">
    <Grid 
        Background="{DynamicResource Toggl.Background}"
        Height="70">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="70" />
            <ColumnDefinition Width="40" />
            <ColumnDefinition Width="10" />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBox 
            x:Name="Description"
            Text="Reviewing Code"
            VerticalAlignment="Bottom"
            Background="Transparent"
            BorderBrush="Transparent"
            BorderThickness="0"
            Grid.Column="1"
            Grid.Row="0" />

        <TextBox 
            x:Name="Project"
            Text="+ Add Project"
            BorderThickness="0"
            Background="Transparent"
            BorderBrush="Transparent"
            Grid.Column="1"
            Grid.Row="2" />

        <TextBox 
            x:Name="Duration"       
            Text="1:00:33"
            BorderThickness="0"
            TextAlignment="Center"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="Transparent"
            BorderBrush="Transparent"
            Grid.Column="2"
            Grid.Row="0" 
            Grid.RowSpan="2"/>

        <Button
            x:Name="StopButton"
            HorizontalAlignment="Center"
            Grid.Column="3"
            Grid.Row="0"
            Grid.RowSpan="2">

            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Ellipse Name="BackgroundEllipse"
                                 Fill="{DynamicResource Toggl.SecondaryAccentBrush}"
                                 Width="36"
                                 Height="36" />
                        <Path Name="StopButton"
                              Width="36"
                              Height="36"
                              Fill="White"
                              Data="{StaticResource StopButtonGeometry}"/>
                        <Ellipse Name="OverlayEllipse"
                                 Width="36"
                                 Height="36"
                                 Fill="Transparent" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OverlayEllipse" Property="Fill">
                                <Setter.Value>
                                    <SolidColorBrush Color="Black" Opacity="0.1" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="OverlayEllipse" Property="Fill">
                                <Setter.Value>
                                    <SolidColorBrush Color="Black" Opacity="0.2" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>


        <Button
            x:Name="StartButton"
            HorizontalAlignment="Center"
            Grid.Column="3"
            Grid.Row="0"
            Grid.RowSpan="2">

            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid>
                        <Ellipse Name="BackgroundEllipse"
                                 Fill="{DynamicResource Toggl.AccentBrush}"
                                 Width="36"
                                 Height="36" />
                        <Path Name="StartButton"
                              Width="36"
                              Height="36"
                              Fill="White"
                              Data="{StaticResource StartButtonGeometry}"/>
                        <Ellipse Name="OverlayEllipse"
                                 Width="36"
                                 Height="36"
                                 Fill="Transparent" />
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OverlayEllipse" Property="Fill">
                                <Setter.Value>
                                    <SolidColorBrush Color="Black" Opacity="0.1" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="OverlayEllipse" Property="Fill">
                                <Setter.Value>
                                    <SolidColorBrush Color="Black" Opacity="0.2" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Button.Template>
        </Button>


    </Grid>
</reactiveUi:ReactiveUserControl>
